# Dépendances de développement / test
pytest>=7.0

# API web (interface React + déploiement Render)
fastapi>=0.110
uvicorn>=0.29

# Visualisation (graphes du simulateur sim/)
matplotlib>=3.5
pillow>=9.0          # requis pour la vue de dessus animée (GIF)

# --- CARLA (uniquement pour l'exécution dans le simulateur, PAS pour les tests) ---
# CARLA n'est en général pas installable via un simple `pip install carla`.
# Utilise le paquet Python livré avec ta version de CARLA, par exemple :
#     pip install <CARLA>/PythonAPI/carla/dist/carla-0.9.15-cp310-*.whl
# ou ajoute ce dossier au PYTHONPATH. Version recommandée : CARLA 0.9.15
# (ScenarioRunner / Leaderboard ne supportent pas encore la branche 0.10.x).
