[pytest]
testpaths = tests
python_files = test_*.py
# La racine du projet est ajoutée au sys.path (mode prepend) grâce à conftest.py,
# ce qui permet aux tests d'importer risk_engine / scenarios / world directement.
